# ADR NNN: Title

## Status

Proposed | Accepted | Superseded by ADR NNN

## Context

What decision needs to be made, and what project constraints matter?

Reference durable sources such as `SPECS.md`, existing code, build behavior, or prior ADRs. Avoid tool-specific assumptions, including instructions that only work in one coding agent.

## Decision

State the chosen approach.

Keep this section short enough that a future contributor or AI coding agent can follow it without reconstructing the whole discussion.

## Consequences

List the practical effects of this decision.

Include tradeoffs, follow-up work, and anything that future changes must preserve.

## Agent Notes

Optional. Use only for coordination details that help multiple coding agents work safely in the repo.

Prefer agent-agnostic instructions:

- Point to canonical files and commands.
- Describe ownership boundaries and expected verification.
- Avoid references to `CLAUDE.md`, Codex-only behavior, or any single-agent memory system.
